import { DeclarationListEmitMode } from "@angular/compiler";

export class Flight {
    flightNr: string;
    flightDateTime: string;
    origin: string;
    destination: string;
    seatsAvailable: string;
    standbysListed: string;
}